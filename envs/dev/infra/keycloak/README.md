## Keycloak (dev)

Plano:
- **Instalação**: Helm chart + values versionado
- **OIDC**: criar realm/clients para ArgoCD e para Kubernetes (se for usar)
- **Ingress/TLS**: hostname fixo (evitar IP) e certificados
- **Persistência**: Postgres e PVCs

