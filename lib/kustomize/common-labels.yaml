apiVersion: builtin
kind: LabelTransformer
metadata:
  name: common-labels
labels:
  managed-by: argocd
fieldSpecs:
  - path: metadata/labels
    create: true

# TODO:
# - Adicionar labels padrão do seu cluster/organização
# - Usar este transformer nos overlays de `envs/*/kustomization.yaml` se quiser

