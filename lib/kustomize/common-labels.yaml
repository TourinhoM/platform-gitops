apiVersion: builtin
kind: LabelTransformer
metadata:
  name: common-labels
labels:
  managed-by: argocd
fieldSpecs:
  - path: metadata/labels
    create: true

# TODO: adicionar labels padrão e usar nos overlays.

